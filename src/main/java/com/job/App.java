package com.job;

import java.util.ArrayList;

/**
 * Hello world!
 *
 */
public class App 
{
    public static void main( String[] args )
    {
        System.out.println( "Hello World!" );
//        result = (x != 0 ? result * 10 : result) + (x != 0 ? last * 10 : last);
//        if (nums[i] * Math.pow(10, String.valueOf(nums[j]).length()) < nums[j] * Math.pow(10, String.valueOf(nums[i]).length())) {
//        }
        //    public String largestNumber(int[] nums) {
//        StringBuilder sb = new StringBuilder();
//        for (int num : nums) {
//            sb.append(num);
//        }
//        char[] chars = sb.toString().toCharArray();
//        for (int i = 0; i < chars.length - 1; i++) {
//            for (int j = i + 1; j < chars.length; j++) {
//                if (chars[j] > chars[i]) {
//                    char temp = chars[i];
//                    chars[i] = chars[j];
//                    chars[j] = temp;
//                }
//            }
//        }
//        sb = new StringBuilder();
//        for (char c : chars) {
//            sb.append(c);
//        }
//        return sb.toString();
//    }

        //        StringBuilder aBs = new StringBuilder(String.valueOf(a));
//        StringBuilder bBs = new StringBuilder(String.valueOf(b));
//        int aFirst = a / (int) Math.pow(10, String.valueOf(a).length() - 1);
//        int bFirst = b / (int) Math.pow(10, String.valueOf(b).length() - 1);
//
//        for (int i = 0; i < String.valueOf(b).length(); i++) {
//            aBs.append(aFirst);
//        }
//        for (int i = 0; i < String.valueOf(a).length(); i++) {
//            bBs.append(bFirst);
//        }
//        return Double.valueOf(bBs.toString()) > Double.valueOf(aBs.toString());
    }
}
